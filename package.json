{
  "name": "personal-ai-roadmap-service",
  "version": "0.1.0",
  "description": "Collaborative Personal Roadmap service for Personal AI",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc && npm run build:web && npm run copy:assets",
    "build:web": "vite build --config web/vite.config.ts",
    "build:server": "tsc && npm run copy:assets",
    "copy:assets": "mkdir -p dist/storage dist/skills/roadmap-planning/references && cp src/storage/schema.sql dist/storage/schema.sql && cp plugin/skills/roadmap-planning/SKILL.md dist/skills/roadmap-planning/SKILL.md && cp plugin/skills/roadmap-planning/references/plan-schema.md dist/skills/roadmap-planning/references/plan-schema.md",
    "dev:web": "vite --config web/vite.config.ts",
    "start": "node dist/server.js",
    "test": "vitest run",
    "deploy": "node tools/deploy.mjs"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@fastify/static": "^8.1.0",
    "better-sqlite3": "^11.7.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.0",
    "nanoid": "^5.1.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.0.0",
    "@vitejs/plugin-vue": "^5.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "vue": "^3.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
